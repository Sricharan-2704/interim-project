[build]
  command = "cd frontend && npm ci && npm run build -- --configuration production --base-href /"
  publish = "frontend/dist/sponsorship-app-frontend"

[context.production.environment]
  # Example env var placeholder; set real values in Netlify UI if needed
  # API_BASE_URL = "https://api.example.com"
